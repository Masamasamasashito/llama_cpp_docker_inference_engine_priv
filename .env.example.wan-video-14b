# ==============================================================================
# 🚀 .env GET READY: Wan 2.2 14B Setup (動画生成・最高品質)
# ==============================================================================
# HOW TO:
# 1. Copy this file to a new file named ".env"
#    Command: cp .env.example.wan-video-14b .env
#
# 2. ComfyUIにWan 2.2モデルとカスタムノードをインストール
#    ComfyUI Manager経由、またはHuggingFaceから手動ダウンロード
#
# 3. Start ComfyUI
#    docker-compose -f docker-compose.comfyui.yml up -d
#
# Note: Generation takes 4-15 minutes per clip with 14B model
# ==============================================================================

# ==============================================================================
# ⚙️ Configuration
# ==============================================================================

# ------------------------------------------------------------------------------
# 1. Global Settings
# ------------------------------------------------------------------------------
COMMON_TIMEZONE=Asia/Tokyo

# ------------------------------------------------------------------------------
# 2. Docker Image & Version (TAG)
# ------------------------------------------------------------------------------
COMMON_LDIE_DOCKER_COMFYUI_IMAGE=ghcr.io/ai-dock/comfyui
COMMON_LDIE_DOCKER_COMFYUI_IMAGE_TAG=latest

# ------------------------------------------------------------------------------
# 3. Network Binding
# ------------------------------------------------------------------------------
DOCKER_HOST_BIND_ADDR=127.0.0.1

# 🔄 IF MULTI-ENV: Change this port (e.g., 8188 -> 18188)
DOCKER_HOST_PORT_COMFYUI=8188
COMFYUI_CONTAINER_LISTEN_PORT=8188  # It is not recommended to change this port

# ------------------------------------------------------------------------------
# 4. Model Settings
# ------------------------------------------------------------------------------
VIDEO_MODEL=wan2.2_14b
VIDEO_MODEL_DESCRIPTION=Wan 2.2 14B (Alibaba, ~14B, ~12-16GB VRAM)
# VRAM: ~12-16GB | Speed: 4-15min/clip | Resolution: up to 720p

# ------------------------------------------------------------------------------
# 5. Generation Parameters
# ------------------------------------------------------------------------------
VIDEO_WIDTH=720
VIDEO_HEIGHT=480
VIDEO_NUM_FRAMES=48
VIDEO_STEPS=30
VIDEO_CFG=7.5
